# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['pakapaka_seq1_v1_touch_new.py'],
    pathex=[],
    binaries=[],
    datas=[('alpacca_sound.wav', '.'), ('pose_landmarker_lite.task', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='pakapaka_seq1_v1_touch_new',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
